server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /prestamos {
        proxy_pass http://backend:3000/prestamos;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
